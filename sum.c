//WAP to calculate sum of two integers. numbers are taken from  user through keyboard.

#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    c=a+b;
    printf("sum is %d",c);
    return 0;
}