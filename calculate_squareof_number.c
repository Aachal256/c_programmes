//WAP to calculate square of a given number. number is entered by the user.

#include<stdio.h>
int main()
{
    int a,k;
    printf("Enter a number:");
    scanf("%d",&a);
    k=a*a;
    printf("square is %d",k);
    return 0;
    

}